Angular 17 Security Settings Module (demo)

Run:

npm install
npm start

This scaffold contains demo components for Allowed IPs, Devices, Policy, OTP, Logs, Session, Alerts. Wire to your backend API endpoints (/api/*) as needed.